# --- Framework ---
fastapi[standard]==0.115.12   # [standard] apporte la CLI `fastapi` (dev/run) + extras
uvicorn[standard]==0.34.2
python-multipart==0.0.20

# --- Database / ORM ---
SQLAlchemy==2.0.40
alembic==1.14.1
psycopg2-binary==2.9.10

# --- Validation / settings ---
pydantic==2.11.4
pydantic-settings==2.7.1
email-validator==2.2.0
python-dotenv==1.1.0

# --- Auth / security ---
PyJWT==2.10.1
cryptography==45.0.5
bcrypt==4.3.0

# --- Tâches planifiées / background (cron in-process, sans Redis) ---
APScheduler==3.11.0

# --- Monitoring ---
sentry-sdk==2.32.0

# --- Tooling ---
ruff==0.12.1
pre-commit==4.2.0

# --- Tests ---
pytest==8.3.4
httpx==0.28.1
