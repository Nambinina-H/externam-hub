# ============================================================
# Externam Hub API — variables d'environnement (apps/backend)
# Copier en `apps/backend/.env` pour un lancement local hors Docker.
# ============================================================

ENVIRONMENT=local

# PostgreSQL
POSTGRES_USER=externam
POSTGRES_PASSWORD=change_me_postgres
POSTGRES_DB=externam
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# Base managée (Railway, etc.) : si défini, prend le dessus sur les POSTGRES_* ci-dessus.
# DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/db

# Seed admin (créé au démarrage s'il n'existe pas)
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=admin1234
SEED_ADMIN_FIRSTNAME=Admin
SEED_ADMIN_LASTNAME=Externam Hub

# JWT (RS256)
JWT_ALGORITHM=RS256
JWT_PRIVATE_KEY_PATH=keys/private.pem
JWT_PUBLIC_KEY_PATH=keys/public.pem
ACCESS_TOKEN_EXPIRATION=900
REFRESH_TOKEN_EXPIRATION=604800

# URLs
BASE_URL=http://localhost:8000
FRONT_BASE_URL=http://localhost:3000

# Tâches planifiées (APScheduler)
SCHEDULER_ENABLED=true

# Monitoring
SENTRY_URL=

# Email (SMTP) — tant que SMTP_USER est vide, send_email() log au lieu d'envoyer (dev hors-ligne)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=noreply@externam-hub.com

# Rapports ads hebdo — heure (0-23) du cron quotidien ; le jour d'envoi est par client
REPORT_SEND_HOUR=9

# Meta Ads (Marketing API) — sans META_ACCESS_TOKEN, le provider reste en stub déterministe
META_GRAPH_VERSION=v25.0
META_ACCESS_TOKEN=
META_APP_ID=
META_APP_SECRET=
# Business partenaire hébergeant l'utilisateur système (token) : découverte auto des
# comptes partagés avec ce business (sinon, seuls les comptes affectés à l'utilisateur système).
META_BUSINESS_ID=
